# 2026-04-27 Daily Log

## 完成工作

### 1. 向量存储从 InMemory 迁移到 PGVector
- pom.xml 添加 `langchain4j-pgvector:0.35.0` 依赖
- VectorStoreConfig.java: InMemoryEmbeddingStore → PgVectorEmbeddingStore
- RetrieverConfig.java: 用 EmbeddingStore<TextSegment> 接口替代具体类
- VectorizationService.java: 用 EmbeddingStore<TextSegment> 接口替代具体类
- application.yml 添加 PGVector 连接配置（支持环境变量覆盖）
- 添加 docker-compose.yml (PostgreSQL 16 + PGVector)

### 2. 修复文件上传 20KB 限制
- request.ts 拦截器: FormData 上传时自动删除 Content-Type
- document.ts: 移除手动设置的 Content-Type header
- request.ts: 全局超时 60s → 300s
- application.yml: 添加 Tomcat max-http-form-post-size 和 max-swallow-size
